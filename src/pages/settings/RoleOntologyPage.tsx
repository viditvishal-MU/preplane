import { GitBranch } from "lucide-react";
import { SettingsStub } from "./SettingsStub";
export default function RoleOntologyPage() {
  return <SettingsStub title="Role Ontology" description="Canonical role taxonomy used by the matching engine." icon={GitBranch} />;
}
